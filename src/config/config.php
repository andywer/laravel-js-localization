<?php

return array(

    /*
    |--------------------------------------------------------------------------
    | Restrict messages
    |--------------------------------------------------------------------------
    |
    | May be set to `false` or an array containing the keys of the messages you
    | wish to make accessible for the Javascript code.
    | Remember that the number of messages sent to the browser influences the
    | time the website needs to load. So you are encouraged to use this option
    | and only add the messages you really need.
    |
    */

    'restrict' => false,

);